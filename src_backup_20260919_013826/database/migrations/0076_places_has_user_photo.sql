-- ========================================================================
-- Migration 0076: מוסיף את עמודת places.has_user_photo, שחסרה עד עכשיו.
--
-- *** תיקון באג: reviewService.ts (createOrUpdateReview) כבר הסתמך על
-- העמודה הזו כדי לדעת אם תמונה שמצורפת לביקורת היא "תמונת המשתמש
-- הראשונה" למקום (ואז מחליפה את image_urls[0], התמונה הראשית) או לא
-- (ואז רק מתווספת לגלריה). בלי המיגרציה הזו, ה-SELECT/UPDATE על
-- has_user_photo נכשלים בשקט (log-only, ר' console.error בקוד) - כל
-- תמונה שמשתמש מצרף לביקורת אף פעם לא נשמרת בפועל ב-places.image_urls,
-- ולכן לא מופיעה לאף משתמש אחר בגלריית התמונות של המקום, גם לא למי
-- שהעלה אותה (בבקשה מפורשת: "יש פה תמונות במקומות האלה - וזה לא מופיע").
--
-- default false: מקומות קיימים שכבר יש להם תמונות ב-image_urls
-- (מה-seed/מ-Google) נחשבים "עדיין בלי תמונת משתמש" - כך שהתמונה
-- הראשונה שמשתמש אמיתי יעלה עדיין תופיע ראשונה, בדיוק לפי ההתנהגות
-- שתועדה בהערה ב-reviewService.ts.
-- ========================================================================

alter table public.places
  add column if not exists has_user_photo boolean not null default false;

comment on column public.places.has_user_photo is
  'true אחרי שמשתמש כלשהו צירף תמונה לביקורת על המקום הזה - אז התמונה שלו כבר הפכה ל-image_urls[0] (הראשית), ותמונות משתמשים נוספות רק יתווספו לגלריה. ר reviewService.ts (createOrUpdateReview).';
