-- ========================================================================
-- Migration 0032: איפוס "מה חדש" - הכל שקיים עכשיו עובר לארכיון
-- (כולל כל מה שנוצר היום בזמן הבדיקות של AI Discovery - סטטוי הליברטי,
-- Central Park וכו') - כדי שהעמוד החדש יתחיל ריק לגמרי מהנקודה הזו.
-- הרצה: Supabase Dashboard -> SQL Editor -> New query -> להדביק ולהריץ
-- לא מוחק שום שורה! רק מסמן is_legacy=true על הכל, נכון לרגע ההרצה.
-- ========================================================================

update public.places set is_legacy = true;
